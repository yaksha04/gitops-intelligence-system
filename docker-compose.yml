version: '3.8'

services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./releases:/app/releases
      - ./ml_model:/app/ml_model
    restart: unless-stopped
